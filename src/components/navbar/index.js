import React from "react"
import { Button, Grid, Box } from 'theme-ui'

const Menu = () => {
    return <Grid columns={1}>
        <Box>
            <Button>The Community</Button>
        </Box>
        <Box>
        </Box>
  </Grid>
}
export default Menu